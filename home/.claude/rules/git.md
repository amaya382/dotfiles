---
description: Applies before `git commit` or `git push`, and when writing a commit message. / `git commit`・`git push` の直前、および commit message を書くときに適用する。
---

# rules for git

- 指示があるまで git commit や git push はしないこと
  - git commit や git push が必要なときは、都度必ず AskUserQuestion で確認すること
- commit message は conventional commits に従い英語で書くこと
