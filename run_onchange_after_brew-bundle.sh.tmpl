#!/bin/bash
# ~/.Brewfile の内容が変わったときだけ実行される
# Brewfile hash: {{ include "dot_Brewfile" | sha256sum }}
set -eu

if ! command -v brew >/dev/null 2>&1; then
  echo "brew not found; skipping brew bundle. Run run_once_install-packages.sh first." >&2
  exit 0
fi

brew bundle --global
